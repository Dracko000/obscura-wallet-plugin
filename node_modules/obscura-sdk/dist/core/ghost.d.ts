import { SentTxResult } from './send';
interface GhostedTxOptions {
    rpcUrl: string;
    privateKey: string;
    to: string;
    valueEth: string;
    fakeCount?: number;
    delayMs?: number;
    mode?: 'serial' | 'parallel';
}
interface GhostedTxResult {
    real: SentTxResult;
    fakes: SentTxResult[];
}
export declare function sendGhostedTx({ rpcUrl, privateKey, to, valueEth, fakeCount, delayMs, mode, }: GhostedTxOptions): Promise<GhostedTxResult>;
export {};
